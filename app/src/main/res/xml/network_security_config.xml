<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to loopback addresses, for local development against
  a server reached via `adb reverse` (localhost) or the emulator's host alias
  (10.0.2.2). Every other destination still requires HTTPS — a real deployment
  should give API_BASE_URL an https:// URL, which needs no special config.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
